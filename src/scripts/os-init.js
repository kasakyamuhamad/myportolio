import AOS from "aos";
import "aos/dist/aos.css";

AOS.init({
  duration: 800, // Animation duration (ms)
  offset: 100,   // How far from the viewport before triggering
  once: true,    // Whether animation should happen only once
});
